# Presentation source

The original generated HTML and Markdown deck for **Types mémoire en .NET** are intentionally not included in this first cleanup PR to keep the initial import small and reviewable.

They can be restored in a follow-up PR after validating the learning-path structure.
